version: "3.8"

services:
  formulo:
    build: .
    ports:
      - "80:5000"
      - "443:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 3500M
